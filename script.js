const myArray = ['yellow',100, true, 'toyota']

let [color, num] = myArray;

console.log(color);

console.log(num);

const newObj = {
    name: nurs,
    addres: {
    town: bishkek,
    steet: Jalal-Abad
    }
}

const {
    name: nurs,
    addres: {town, steet},
} = newObj 

console.log(town);